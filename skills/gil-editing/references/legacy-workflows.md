# Legacy Workflows

Some workflows still live in `genshin-ts` and are not part of openGil v1:

- MediaPipe pose extraction and preview rendering.
- VMD parsing and stickman motion conversion.
- Blender/MMD JSON animation import scripts.
- Historical JSON IR dump/rebuild tools.

Use them as research references or oracle outputs while openGil migrates verified binary atomics.

